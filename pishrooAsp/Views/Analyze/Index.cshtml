<!DOCTYPE html>
<html dir="rtl" lang="fa">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>آمار بازدیدها</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        'vazir': ['Vazirmatn', 'sans-serif'],
                    },
                    colors: {
                        primary: {
                            50: '#eff6ff',
                            500: '#3b82f6',
                            600: '#2563eb',
                            700: '#1d4ed8',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        body {
            font-family: 'Vazirmatn', sans-serif;
        }
    </style>
</head>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<body class="bg-gray-50 font-vazir">
    <div class="container mx-auto px-4 py-8">
        <!-- هدر صفحه -->
        <div class="text-center mb-12">
            <h1 class="text-3xl md:text-4xl font-bold text-gray-800 mb-4">📊 آمار بازدیدها</h1>
            <p class="text-gray-600 max-w-2xl mx-auto">نمایش جامع آمار بازدیدکنندگان سایت با جزئیات کامل و نمودارهای تعاملی</p>
        </div>

        <!-- کارت‌های آمار کلی -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-12">
            <!-- کل بازدیدها -->
            <div class="bg-white rounded-2xl shadow-lg p-6 border-r-4 border-blue-500 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
                <div class="flex items-center justify-between mb-4">
                    <div class="bg-blue-100 p-3 rounded-full">
                        <i class="fas fa-users text-blue-600 text-xl"></i>
                    </div>
                    <span class="text-sm font-medium text-blue-600 bg-blue-100 px-2 py-1 rounded-full">+12%</span>
                </div>
                <h3 class="text-gray-500 text-sm font-medium mb-1">کل بازدیدها</h3>
                <p class="text-2xl font-bold text-gray-800">@Model.TotalVisits.ToString("N0")</p>
                <div class="mt-4 h-2 bg-gray-200 rounded-full overflow-hidden">
                    <div class="h-full bg-blue-500 rounded-full" style="width: 85%"></div>
                </div>
            </div>

            <!-- بازدیدهای منحصر به فرد -->
            <div class="bg-white rounded-2xl shadow-lg p-6 border-r-4 border-green-500 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
                <div class="flex items-center justify-between mb-4">
                    <div class="bg-green-100 p-3 rounded-full">
                        <i class="fas fa-user-check text-green-600 text-xl"></i>
                    </div>
                    <span class="text-sm font-medium text-green-600 bg-green-100 px-2 py-1 rounded-full">+8%</span>
                </div>
                <h3 class="text-gray-500 text-sm font-medium mb-1">بازدیدهای منحصر به فرد</h3>
                <p class="text-2xl font-bold text-gray-800">@Model.UniqueVisits.ToString("N0")</p>
                <div class="mt-4 h-2 bg-gray-200 rounded-full overflow-hidden">
                    <div class="h-full bg-green-500 rounded-full" style="width: 65%"></div>
                </div>
            </div>

            <!-- بازدیدهای امروز -->
            <div class="bg-white rounded-2xl shadow-lg p-6 border-r-4 border-purple-500 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
                <div class="flex items-center justify-between mb-4">
                    <div class="bg-purple-100 p-3 rounded-full">
                        <i class="fas fa-calendar-day text-purple-600 text-xl"></i>
                    </div>
                    <span class="text-sm font-medium text-purple-600 bg-purple-100 px-2 py-1 rounded-full">+24%</span>
                </div>
                <h3 class="text-gray-500 text-sm font-medium mb-1">بازدیدهای امروز</h3>
                <p class="text-2xl font-bold text-gray-800">@Model.TodayVisits.ToString("N0")</p>
                <div class="mt-4 h-2 bg-gray-200 rounded-full overflow-hidden">
                    <div class="h-full bg-purple-500 rounded-full" style="width: 45%"></div>
                </div>
            </div>

            <!-- بازدیدهای منحصر به فرد امروز -->
            <div class="bg-white rounded-2xl shadow-lg p-6 border-r-4 border-yellow-500 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
                <div class="flex items-center justify-between mb-4">
                    <div class="bg-yellow-100 p-3 rounded-full">
                        <i class="fas fa-star text-yellow-600 text-xl"></i>
                    </div>
                    <span class="text-sm font-medium text-yellow-600 bg-yellow-100 px-2 py-1 rounded-full">+15%</span>
                </div>
                <h3 class="text-gray-500 text-sm font-medium mb-1">بازدیدهای منحصر به فرد امروز</h3>
                <p class="text-2xl font-bold text-gray-800">@Model.TodayUniqueVisits.ToString("N0")</p>
                <div class="mt-4 h-2 bg-gray-200 rounded-full overflow-hidden">
                    <div class="h-full bg-yellow-500 rounded-full" style="width: 55%"></div>
                </div>
            </div>
        </div>

        <!-- بخش نمودارها و جداول -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-12">
            <!-- نمودار آمار دستگاه‌ها -->
            <div class="bg-white rounded-2xl shadow-lg p-6">
                <div class="flex justify-between items-center mb-6">
                    <h2 class="text-xl font-bold text-gray-800">📱 آمار دستگاه‌ها</h2>
                    <div class="bg-gray-100 text-gray-600 px-3 py-1 rounded-full text-sm">
                        <i class="fas fa-filter mr-1"></i> فیلتر
                    </div>
                </div>
                <div class="h-80">
                    <canvas id="deviceChart"></canvas>
                </div>
            </div>

            <!-- نمودار صفحات محبوب -->
            <div class="bg-white rounded-2xl shadow-lg p-6">
                <div class="flex justify-between items-center mb-6">
                    <h2 class="text-xl font-bold text-gray-800">🔥 صفحات محبوب</h2>
                    <div class="bg-gray-100 text-gray-600 px-3 py-1 rounded-full text-sm">
                        <i class="fas fa-sort mr-1"></i> مرتب‌سازی
                    </div>
                </div>
                <div class="h-80">
                    <canvas id="pagesChart"></canvas>
                </div>
            </div>
        </div>

        <!-- آمار ماه جاری و جدول جزئیات -->
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- آمار ماه جاری -->
            <div class="bg-gradient-to-r from-blue-500 to-purple-600 rounded-2xl shadow-lg p-6 text-white lg:col-span-1">
                <div class="flex items-center mb-4">
                    <div class="bg-white bg-opacity-20 p-3 rounded-full mr-3">
                        <i class="fas fa-chart-line text-xl"></i>
                    </div>
                    <h2 class="text-xl font-bold">📈 آمار ماه جاری</h2>
                </div>
                <div class="text-center py-6">
                    <p class="text-5xl font-bold mb-2">@Model.ThisMonthVisits.ToString("N0")</p>
                    <p class="text-blue-100">بازدید کل در ماه جاری</p>
                </div>
                <div class="mt-4 flex items-center justify-between">
                    <div class="text-center">
                        <p class="text-2xl font-bold">@((Model.ThisMonthVisits / 30).ToString("N0"))</p>
                        <p class="text-sm text-blue-100">میانگین روزانه</p>
                    </div>
                    <div class="text-center">
                        <p class="text-2xl font-bold">+18%</p>
                        <p class="text-sm text-blue-100">رشد نسبت به ماه قبل</p>
                    </div>
                </div>
            </div>

            <!-- جدول جزئیات دستگاه‌ها -->
            <div class="bg-white rounded-2xl shadow-lg p-6 lg:col-span-2">
                <h2 class="text-xl font-bold text-gray-800 mb-6">جزئیات آمار دستگاه‌ها</h2>
                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead>
                            <tr class="border-b border-gray-200">
                                <th class="py-3 px-4 text-right text-gray-600 font-medium">نوع دستگاه</th>
                                <th class="py-3 px-4 text-right text-gray-600 font-medium">تعداد</th>
                                <th class="py-3 px-4 text-right text-gray-600 font-medium">درصد</th>
                                <th class="py-3 px-4 text-right text-gray-600 font-medium">روند</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var device in Model.DeviceStats)
                            {
                                <tr class="border-b border-gray-100 hover:bg-gray-50 transition-colors">
                                    <td class="py-3 px-4">
                                        <div class="flex items-center">
                                            @if (device.DeviceType == "موبایل")
                                            {
                                                <i class="fas fa-mobile-alt text-blue-500 ml-2"></i>
                                            }
                                            else if (device.DeviceType == "دسکتاپ")
                                            {
                                                <i class="fas fa-desktop text-purple-500 ml-2"></i>
                                            }
                                            else
                                            {
                                                <i class="fas fa-tablet-alt text-green-500 ml-2"></i>
                                            }
                                            <span>@device.DeviceType</span>
                                        </div>
                                    </td>
                                    <td class="py-3 px-4 font-medium">@device.Count.ToString("N0")</td>
                                    <td class="py-3 px-4">
                                        <div class="flex items-center">
                                            <span class="ml-2">@device.Percentage.ToString("0.0")%</span>
                                            <div class="w-16 bg-gray-200 rounded-full h-2">
                                                <div class="bg-blue-500 h-2 rounded-full" style="width: @device.Percentage%"></div>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="py-3 px-4">
                                        @if (device.Percentage > 30)
                                        {
                                            <span class="text-green-500 text-sm font-medium"><i class="fas fa-arrow-up ml-1"></i> افزایش</span>
                                        }
                                        else
                                        {
                                            <span class="text-red-500 text-sm font-medium"><i class="fas fa-arrow-down ml-1"></i> کاهش</span>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- جدول صفحات محبوب -->
        <div class="bg-white rounded-2xl shadow-lg p-6 mt-8">
            <h2 class="text-xl font-bold text-gray-800 mb-6">جزئیات صفحات محبوب</h2>
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead>
                        <tr class="border-b border-gray-200">
                            <th class="py-3 px-4 text-right text-gray-600 font-medium">صفحه</th>
                            <th class="py-3 px-4 text-right text-gray-600 font-medium">بازدید کل</th>
                            <th class="py-3 px-4 text-right text-gray-600 font-medium">بازدید منحصر به فرد</th>
                            <th class="py-3 px-4 text-right text-gray-600 font-medium">نرخ تبدیل</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var pages in Model.PopularPages)
                        {
                            <tr class="border-b border-gray-100 hover:bg-gray-50 transition-colors">
                                <td class="py-3 px-4">
                                    <div class="flex items-center">
                                        <i class="fas fa-file-alt text-blue-500 ml-2"></i>
                                        <span class="truncate max-w-xs">@pages.Path</span>
                                    </div>
                                </td>
                                <td class="py-3 px-4 font-medium">@pages.VisitCount.ToString("N0")</td>
                                <td class="py-3 px-4">@pages.UniqueVisitCount.ToString("N0")</td>
                                <td class="py-3 px-4">
                                    @{
                                        var conversionRate = pages.UniqueVisitCount > 0 ? (pages.VisitCount / (double)pages.UniqueVisitCount * 100).ToString("0.0") : "0.0";
                                    }
                                    <span class="bg-green-100 text-green-800 text-xs font-medium px-2 py-1 rounded-full">@conversionRate%</span>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <script>
        // داده‌های واقعی برای نمودار دستگاه‌ها
        const deviceLabels = [
            @foreach (var device in Model.DeviceStats)
            {
                @:'@device.DeviceType',
            }
        ];

        const deviceData = [
            @foreach (var device in Model.DeviceStats)
            {
                @:@device.Count,
            }
        ];

        // داده‌های واقعی برای نمودار صفحات محبوب (۵ صفحه اول)
        const pagesLabels = [
        @foreach (var pages in Model.PopularPages)
            {
                @:'@pages.Path',
            }
        ];

        const pagesTotalData = [
        @foreach (var pages in Model.PopularPages)
            {
                @:@pages.VisitCount,
            }
        ];

        const pagesUniqueData = [
        @foreach (var pages in Model.PopularPages)
            {
                @:@pages.UniqueVisitCount,
            }
        ];

        // نمودار آمار دستگاه‌ها
        const deviceCtx = document.getElementById('deviceChart').getContext('2d');
        const deviceChart = new Chart(deviceCtx, {
            type: 'doughnut',
            data: {
                labels: deviceLabels,
                datasets: [{
                    data: deviceData,
                    backgroundColor: [
                        'rgba(59, 130, 246, 0.8)',
                        'rgba(139, 92, 246, 0.8)',
                        'rgba(16, 185, 129, 0.8)',
                        'rgba(245, 158, 11, 0.8)',
                        'rgba(239, 68, 68, 0.8)'
                    ],
                    borderColor: [
                        'rgb(59, 130, 246)',
                        'rgb(139, 92, 246)',
                        'rgb(16, 185, 129)',
                        'rgb(245, 158, 11)',
                        'rgb(239, 68, 68)'
                    ],
                    borderWidth: 2
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'bottom',
                        rtl: true,
                        labels: {
                            font: {
                                family: 'Vazirmatn'
                            },
                            padding: 20
                        }
                    },
                    tooltip: {
                        rtl: true,
                        bodyFont: {
                            family: 'Vazirmatn'
                        },
                        titleFont: {
                            family: 'Vazirmatn'
                        }
                    }
                }
            }
        });

        // نمودار صفحات محبوب
        const pagesCtx = document.getElementById('pagesChart').getContext('2d');
        const pagesChart = new Chart(pagesCtx, {
            type: 'bar',
            data: {
                labels: pagesLabels,
                datasets: [{
                    label: 'بازدید کل',
                    data: pagesTotalData,
                    backgroundColor: 'rgba(59, 130, 246, 0.7)',
                    borderColor: 'rgb(59, 130, 246)',
                    borderWidth: 1
                }, {
                    label: 'بازدید منحصر به فرد',
                    data: pagesUniqueData,
                    backgroundColor: 'rgba(16, 185, 129, 0.7)',
                    borderColor: 'rgb(16, 185, 129)',
                    borderWidth: 1
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                scales: {
                    x: {
                        grid: {
                            display: false
                        },
                        ticks: {
                            font: {
                                family: 'Vazirmatn'
                            }
                        }
                    },
                    y: {
                        beginAtZero: true,
                        grid: {
                            color: 'rgba(0, 0, 0, 0.05)'
                        },
                        ticks: {
                            font: {
                                family: 'Vazirmatn'
                            }
                        }
                    }
                },
                plugins: {
                    legend: {
                        position: 'top',
                        rtl: true,
                        labels: {
                            font: {
                                family: 'Vazirmatn'
                            }
                        }
                    },
                    tooltip: {
                        rtl: true,
                        bodyFont: {
                            family: 'Vazirmatn'
                        },
                        titleFont: {
                            family: 'Vazirmatn'
                        }
                    }
                }
            }
        });
    </script>
</body>
</html>